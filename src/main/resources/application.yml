spring:
  h2:
    console:
      enabled: true
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test
    username: spring
    password:
  session:
    store-type: redis
    timeout: 60000
    redis:
      namespace: spring:session:tommy
